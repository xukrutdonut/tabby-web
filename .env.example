# Tabby Web Configuration for Raspberry Pi 5 / Docker
# Copy this file to .env and fill in the values

# ===================
# Database Configuration
# ===================
# For MariaDB (recommended for RPi5)
DATABASE_URL=mysql://root:CHANGE_ME@db/tabby

# For PostgreSQL (alternative)
# DATABASE_URL=postgresql://user:password@db/tabby

# For SQLite (not recommended for production)
# DATABASE_URL=sqlite:///db.sqlite3

# ===================
# Django Configuration
# ===================
# IMPORTANT: Generate a secure secret key for production
# You can generate one with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=CHANGE_ME_TO_A_SECURE_RANDOM_STRING

# Set to False in production
DEBUG=False

# ===================
# Domain & URL Configuration
# ===================
# Your domain behind Nginx Proxy Manager
FRONTEND_URL=https://tabby.serviciosylaboratoriodomestico.site
BACKEND_URL=https://tabby.serviciosylaboratoriodomestico.site

# Alternative: if frontend and backend are on different domains
# CORS_EXTRA_URL=https://tabby.serviciosylaboratoriodomestico.site

# ===================
# OAuth Authentication
# ===================
# You MUST configure at least one OAuth provider for authentication
# GitHub OAuth (recommended)
SOCIAL_AUTH_GITHUB_KEY=your_github_oauth_key
SOCIAL_AUTH_GITHUB_SECRET=your_github_oauth_secret

# GitLab OAuth (optional)
# SOCIAL_AUTH_GITLAB_KEY=your_gitlab_oauth_key
# SOCIAL_AUTH_GITLAB_SECRET=your_gitlab_oauth_secret

# Google OAuth (optional)
# SOCIAL_AUTH_GOOGLE_OAUTH2_KEY=your_google_oauth_key
# SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET=your_google_oauth_secret

# Microsoft OAuth (optional)
# SOCIAL_AUTH_MICROSOFT_GRAPH_KEY=your_microsoft_oauth_key
# SOCIAL_AUTH_MICROSOFT_GRAPH_SECRET=your_microsoft_oauth_secret

# ===================
# Application Storage
# ===================
# Storage for Tabby distributions
# Local storage (default)
APP_DIST_STORAGE=file:///app-dist

# S3 storage (optional)
# APP_DIST_STORAGE=s3://bucket-name/path

# GCS storage (optional)
# APP_DIST_STORAGE=gcs://bucket-name/path

# NPM Registry (default is npmjs.org)
NPM_REGISTRY=https://registry.npmjs.org

# ===================
# Tabby Connection Gateway
# ===================
# Gateway configuration for SSH/Telnet connections
# If you're running tabby-connection-gateway, configure these
# CONNECTION_GATEWAY_AUTH_CA=/path/to/ca.crt
# CONNECTION_GATEWAY_AUTH_CERTIFICATE=/path/to/cert.crt
# CONNECTION_GATEWAY_AUTH_KEY=/path/to/key.key

# ===================
# Docker Configuration
# ===================
# Port mapping (internal container port is 80)
TABBY_PORT=9090

# Dockerize wait configuration for database
DOCKERIZE_ARGS=-wait tcp://db:3306 -timeout 60s

# ===================
# Database Service Configuration (for MariaDB)
# ===================
MARIADB_DATABASE=tabby
MARIADB_USER=user
MARIADB_PASSWORD=CHANGE_ME
MYSQL_ROOT_PASSWORD=CHANGE_ME

# ===================
# Optional: Analytics
# ===================
# Google Analytics (optional)
# GA_ID=your_ga_tracking_id
# GA_DOMAIN=tabby.serviciosylaboratoriodomestico.site

# ===================
# Optional: GitHub Sponsors
# ===================
# GITHUB_ELIGIBLE_SPONSORSHIPS=sponsor1,sponsor2
# GITHUB_SPONSORS_MIN_PAYMENT=500
